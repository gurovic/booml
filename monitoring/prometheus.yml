global:
  scrape_interval: 15s
  evaluation_interval: 15s

scrape_configs:
  - job_name: booml-backend
    metrics_path: /prometheus/metrics
    static_configs:
      - targets:
          - backend:8100

  - job_name: prometheus
    static_configs:
      - targets:
          - prometheus:9090
