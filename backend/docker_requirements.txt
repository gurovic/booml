# Lightweight base packages preinstalled in the runner VM image.
# Keep this list short so builds stay fast; users can pip install extras in a session.
numpy==2.1.3
pandas==2.3.3
scipy==1.16.3
scikit-learn==1.7.2
requests==2.32.5
